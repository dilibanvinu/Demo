# Workflow Name 
name: Firsr Workflow

#Trigger
on:
 push:
   workflow_dispatch:

jobs:
# First job
Hellow-world:
  runs-on: ubuntu-latest

  steps:
  - name: Hello World
    run: echo "Hellow World"
  - name: Event Name 
    run: echo "Event Name :${{ github.event_name }}"
  
    
   

   
