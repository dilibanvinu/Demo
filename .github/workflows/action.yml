name: First Workflow

on:
  push:
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        run: echo "Hello World"

      - name: Event Name
        run: echo "Event Name: ${{ github.event_name }}"
