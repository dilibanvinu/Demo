# Workflow Name 
name: Firsr Workflow

#Trigger
on:
 push:
   workflow_dispatch:

jobs:
# First job
Hello-world:
  runs-on: ubuntu-latest

  steps:
  - name: Hello World
    run: echo "Hello World"
    
  - name: Event Name 
    run: echo "Event Name:${{ github.event_name }}"
  
    
   

   
